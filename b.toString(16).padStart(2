✓ 资源云 - 一站式网盘资源聚合管理平台
  http://localhost:5173/
✓ Done
"crypto.subtle.digest('SHA-256', new TextEncoder().encode('admin123')).then(buf = const arr = Array.from(new Uint8Array(buf)); return arr.map(b =, '0')).join('') })"
