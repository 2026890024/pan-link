# Cloudflare D1 + Pages 配置

# API 基础地址（生产环境留空 = 同域部署的 Pages Functions）
# 开发环境可设置完整地址，如 https://pan-link-api.xxx.workers.dev
VITE_API_BASE_URL=

# 部署说明：
# 1. 创建 Cloudflare D1 数据库: wrangler d1 create pan-link-db
# 2. 在 D1 控制台执行 d1-schema.sql 建表
# 3. 部署 Pages: wrangler pages deploy app/dist
# 4. 在 Pages → Settings → Functions 绑定 D1 (变量名: DB)
# 5. 在 Pages → Settings → Environment variables 设置:
#    ADMIN_USER=your_admin_username
#    ADMIN_PASS=your_admin_password
#    JWT_SECRET=your_random_secret_key
#
# 本地开发：
#   cd app && npm run dev
#   默认使用 localStorage 模式，无需配置环境变量
#   如需测试 API: npx wrangler pages dev -- npm run dev
